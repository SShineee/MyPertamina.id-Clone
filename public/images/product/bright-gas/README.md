Taruh gambar produk Bright Gas di sini, contoh nama file:

- `scanner.png` — gambar tabung + HP scan QR code, dipakai di `BrightGasScanner.vue` (section "Bright Gas Scanner")

Selama file belum ada, komponen terkait akan menampilkan placeholder abu-abu bergaris putus-putus (`ImagePlaceholder.vue`).
