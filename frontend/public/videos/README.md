Taruh file video hero di sini dengan nama `hero-spbu.mp4` (format MP4, disarankan H.264, durasi pendek/loop, ukuran dikompres agar ringan).

Komponen `HeroSection.vue` akan otomatis memuatnya dari path `/videos/hero-spbu.mp4`.
